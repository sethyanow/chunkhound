# Session Handoff — 2026-04-02

## Tasks Ready
- ch-tox: Phase 3 Acceptance: Primitive MCP Tools (active, owner: Seth)

## Decisions Made
- **Demo script is theater — live MCP calls are the real acceptance demo.** The script (`scripts/demo_lsp.py`) was designed to import and call MCP tool internals directly. Multiple sessions have failed trying to make it work: raw DuckDB lock contention with the MCP server, `execute_query` not supporting DML, `create_services` fighting with running MCP processes, incremental refresh needing raw DB access the service layer doesn't expose. The user called it "completely worthless theater." The live MCP tool calls in conversation ARE the demo.


## Context for Next Session

### ch-tox acceptance demo was run live via MCP. Results:

**BUGS FOUND :**
1. **graph(boundary) returns garbage** — scope="chunkhound/lsp/" returns 10 identical `__all__` self-references from `cli/utils/__init__.py`. The boundary SQL is broken. None of the results actually cross the scope boundary.
2. **LSP tool uses 0-based line/character in responses** — returns `line: 31, character: 6` when every editor and tool shows `line 32, char 7`. Input is 0-based (matching LSP protocol), output should be 1-based (in text files you start at line 1 character 1). Affects `location_to_dict`, `call_item_to_dict`, `diagnostic_to_dict` in `formatters.py`. The `lsp_impl` input params also need converting (accept 1-based, subtract 1 before calling LSP client).
3. **graph(walk) class-level FQN has no edges** — `LSPClient` returns 0 edges, but `LSPClient::start` works fine. Class-level symbols aren't wired as edge sources — only their members are. This is an edge population gap, not a tool bug.
