# Session Handoff — 2026-04-02

## Changed Files
- .bones/tasks/ch-tox.md (+54 -49)
- scripts/demo_lsp.py (+35 -11)

Recent commits:
- 99b5b014 fix(daemon): wire lsp_client_pool through daemon tool dispatch

## Tasks Open
- ch-tox: Phase 3 Acceptance: Primitive MCP Tools (active, owned by Seth)
- ch-zyz: Phase 3: Primitive MCP Tools (epic, blocked by ch-tox)
- ch-8e7: LSP + Graph Intelligence Layer (parent epic)
- ch-dar: Phase 4: Fusion MCP Tools
- ch-z2o: Phase 5: Search Pipeline + code_research
- ch-bf0: Phase 6: Skill Script Library

## Tasks Ready
- ch-tox: Phase 3 Acceptance: Primitive MCP Tools

## Decisions Made
- Daemon LSP pool wiring was missing — `daemon/server.py:_handle_tools_call` didn't pass `lsp_client_pool=self._lsp_pool` to `handle_tool_call()`. Stdio path had it, daemon path didn't. Fixed, committed, verified via MCP. This was the root cause of all LSP tools returning "lsp_not_ready" through the daemon proxy.
- Demo script Phase 3 sections were initially built with raw SQL duplicating tools.py queries. User correctly identified this bypasses the MCP tool internals and defeats the shakedown purpose. Sections need rewrite to go through `create_services()` + `execute_tool()`.
- AGENTS.md and CLAUDE.md updates removed from acceptance scope — tool docs don't belong in AGENTS.md (routing hints only, tool docs go in tool descriptions or hooks), and CLAUDE.md needs no Phase 3 changes.
- `type_filter="Result"` changed to `type_filter="str"` in demo scenarios — Python codebase, not Rust.

## Context for Next Session

**ch-tox is mid-execution.** Daemon fix is committed. Demo script needs rewrite.

**Demo script rewrite (Step 2):** Current Phase 3 sections (11-13: search_symbols, graph_walk, graph_boundary) use raw DuckDB SQL. LSP sections (14-16: lsp_definition, lsp_references, symbol_context) call execute_tool but passed `services=None` and targeted empty line positions. ALL Phase 3 sections need to use `create_services()` from `database_factory.py` to get real services, then call `execute_tool()`. The DB lock from the MCP server holding the connection is a potential issue — if `create_services()` can't open the DB, that's a finding to surface, not work around.

**graph(walk) bug found during demo run:** `graph(walk, symbol="LSPClient::start")` returns 0 edges despite overview showing 28 edges. Root cause: the walk CTE joins on `from_fqn` (outbound only), but all 28 edges are inbound (`to_fqn = 'LSPClient::start'` from `findReferences`). The population creates edges where references point TO the symbol, not FROM the symbol. The walk only follows outbound. This affects every symbol — not just LSPClient. This needs a bug filed separately from the acceptance task. User has not yet decided what to do with it.

**Demo run results (first run, pre-rewrite):**
- Phase 1-2: all PASS (except incremental_refresh — DB locked by MCP, expected)
- search_symbols: PASS (10 results for "parse")
- graph_walk: FAIL (0 edges — bug above)
- graph_boundary: PASS (20 cross-scope edges)
- lsp_definition: FAIL (targeted empty position — demo bug)
- lsp_references: FAIL (same)
- symbol_context: FAIL (services=None crash — demo bug)

**Memory files written this session:** feedback_agents_md_scope.md, feedback_acceptance_is_user_gate.md, reference_phase3_demo_findings.md (update this after rewrite).

**User feedback captured:** Don't rush acceptance tasks to check boxes. Don't classify failures as "known issues" — that's triage and belongs to the user. Don't create fake wrappers that bypass internals. Demo script drives through the real service stack or it's pointless.
