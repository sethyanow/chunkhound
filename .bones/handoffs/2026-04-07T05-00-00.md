# Session Handoff — 2026-04-07

## Changed Files
- .bones/tasks/ch-eun.md (+195 -19)

## Decisions Made
- **Tier definitions are strict.** "Fast enough" is not unit. If it calls DuckDB, LanceDB, tree-sitter, git, or filesystem for real, it needs fixtures or mocks. No rationalizing away the definitions. This was a hard correction mid-session — first audit attempt incorrectly classified ~80 files as "remark unit" by calling external calls "fast and deterministic." Rejected.
- **DuckDB/LanceDB provider tests get fixtures, not deletion.** The response is deterministic — fixture it. We're not "testing DuckDB" — we're testing our SQL/provider logic. But we don't need a live engine to verify deterministic responses.
- **Provider contract tests vs service-level tests use different fixture approaches.** Provider tests (test_duckdb_graph_protocol, etc.) capture method response fixtures. Service tests (test_coordinator_per_file_txn, etc.) swap to FakeDatabaseProvider.
- **0 deletions.** Every integration-marked test tests our logic, not the external system's behavior.
- **FakeDatabaseProvider follows existing pattern.** `tests/fixtures/fake_providers.py` already has FakeEmbeddingProvider, FakeLLMProvider. Same approach for database.
- **Semantic assertions preserved.** Tests that assert "chunk contains symbol X" are stronger than snapshot comparison. Fixture conversion must preserve semantic assertions, not replace with `assert chunks == fixture_chunks`.

## Context for Next Session
- **ch-eun Phase 1 (audit) is complete.** Skeleton at `.bones/tasks/ch-eun.md` has everything: all 118 files classified by name, fixture boundaries per category, FakeDatabaseProvider contract (method list), conftest fixtures that need updating, shared test helpers, pytest config, tmp_path boundary rule.
- **Next step is Phase 2: build infrastructure.** FakeDatabaseProvider class, parser fixture capture utility, git subprocess mock. Then Phase 3 (remark-only: 28→unit, 11→e2e), Phase 4 (convert 67 files), Phase 5 (verify).
- **Key conftest dependencies.** `tests/integration/conftest.py` has `lancedb_provider` fixture that creates real LanceDBProvider — all LanceDB integration tests use it. Must update or replace. `tests/ignore/conftest.py` has `_require_git_available` — converted tests must not depend on it.
- **provider_configs.py is already fake-only.** `get_reranking_providers()` returns only `FakeEmbeddingProvider`. No real API calls.
- **ch-eun skeleton changes are uncommitted.** The +195 line diff to `.bones/tasks/ch-eun.md` needs committing.
- **ch-nxu is also active** (graph layer abstraction) but lower priority. Don't mix work.
- **User feedback (critical, recurring across sessions):** Agent keeps reading tier definitions / requirements and then substituting its own "pragmatic" interpretation. The definitions are the definitions. Follow them exactly. This is the single most important behavioral correction.
